<Window x:Class="TrussModelling.PileEccentricityCalculationSheetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pile Ecc Point Load Calculation Sheet"
        Width="1160"
        Height="780"
        MinWidth="900"
        MinHeight="620"
        WindowStartupLocation="CenterOwner"
        Background="#f8fafc">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#0f172a"/>
        </Style>
        <Style x:Key="MutedText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#64748b"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        <Style x:Key="StepLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,3"/>
        </Style>
        <Style x:Key="StepValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#0f172a"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <Border DockPanel.Dock="Top"
                Background="#0f172a"
                Padding="16,12">
            <DockPanel>
                <Button DockPanel.Dock="Right"
                        Content="Close"
                        Width="84"
                        Margin="12,0,0,0"
                        Click="Close_Click"/>
                <StackPanel>
                    <TextBlock Text="{Binding Title}"
                               Foreground="White"
                               FontSize="18"
                               FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Subtitle}"
                               Foreground="#cbd5e1"
                               Margin="0,3,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="18">
                <ItemsControl ItemsSource="{Binding Sections}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White"
                                    BorderBrush="#cbd5e1"
                                    BorderThickness="1"
                                    Padding="14"
                                    Margin="0,0,0,14">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}"
                                               FontSize="16"
                                               FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Description}"
                                               Style="{StaticResource MutedText}"
                                               Margin="0,4,0,10"/>

                                    <ItemsControl ItemsSource="{Binding Steps}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border BorderBrush="#e2e8f0"
                                                        BorderThickness="0,1,0,0"
                                                        Padding="0,10,0,8">
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding Item}"
                                                                   FontWeight="SemiBold"
                                                                   TextWrapping="Wrap"/>
                                                        <Grid Margin="0,8,0,0">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="1.1*"/>
                                                                <ColumnDefinition Width="1.7*"/>
                                                                <ColumnDefinition Width="1.1*"/>
                                                            </Grid.ColumnDefinitions>

                                                            <StackPanel Grid.Column="0"
                                                                        Margin="0,0,14,0">
                                                                <TextBlock Text="Formula"
                                                                           Style="{StaticResource StepLabel}"/>
                                                                <TextBlock Text="{Binding Formula}"
                                                                           Style="{StaticResource StepValue}"/>
                                                            </StackPanel>

                                                            <StackPanel Grid.Column="1"
                                                                        Margin="0,0,14,0">
                                                                <TextBlock Text="Substitution"
                                                                           Style="{StaticResource StepLabel}"/>
                                                                <TextBlock Text="{Binding Substitution}"
                                                                           Style="{StaticResource StepValue}"/>
                                                            </StackPanel>

                                                            <StackPanel Grid.Column="2">
                                                                <TextBlock Text="Result"
                                                                           Style="{StaticResource StepLabel}"/>
                                                                <TextBlock Text="{Binding Result}"
                                                                           Style="{StaticResource StepValue}"
                                                                           FontWeight="SemiBold"/>
                                                            </StackPanel>
                                                        </Grid>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Border Background="White"
                        BorderBrush="#cbd5e1"
                        BorderThickness="1"
                        Padding="14"
                        Margin="0,0,0,14"
                        Visibility="{Binding HasComparisons, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="Before / After Summary"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,10"/>
                        <DataGrid ItemsSource="{Binding Comparisons}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  CanUserAddRows="False"
                                  GridLinesVisibility="Horizontal"
                                  HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Group" Binding="{Binding GroupId}" Width="80"/>
                                <DataGridTextColumn Header="Case" Binding="{Binding CaseName}" Width="150"/>
                                <DataGridTextColumn Header="Rv kN" Binding="{Binding RvkN, StringFormat=0.##}" Width="90"/>
                                <DataGridTextColumn Header="Mx rem" Binding="{Binding MxRemainingkNm, StringFormat=0.##}" Width="90"/>
                                <DataGridTextColumn Header="My rem" Binding="{Binding MyRemainingkNm, StringFormat=0.##}" Width="90"/>
                                <DataGridTextColumn Header="Max Pile" Binding="{Binding MaxPileLoadkN, StringFormat=0.##}" Width="95"/>
                                <DataGridTextColumn Header="Min Pile" Binding="{Binding MinPileLoadkN, StringFormat=0.##}" Width="95"/>
                                <DataGridTextColumn Header="Uplift" Binding="{Binding Uplift}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <Border Background="White"
                        BorderBrush="#cbd5e1"
                        BorderThickness="1"
                        Padding="14"
                        Visibility="{Binding HasMessages, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="Warnings and Notes"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,10"/>
                        <ItemsControl ItemsSource="{Binding Messages}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding SeverityText}"
                                                   FontWeight="SemiBold"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Message}"
                                                   TextWrapping="Wrap"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>
